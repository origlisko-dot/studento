{
  "name": "telegram-tv-cast-server",
  "version": "1.0.0",
  "private": true,
  "description": "Pairing + media-proxy backend for Telegram TV Cast (one public bot, no per-user bot setup).",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "set-webhook": "node set-webhook.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
